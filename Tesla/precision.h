//Handles data types. So if data types need to be a changed it can be done here.
#ifndef _PRECISION_H
#define _PRECISION_H
namespace Tesla
{
	typedef float Real;
	typedef unsigned long int uInt32;
	typedef signed int Int32;
}
#endif
